import OpenAI from 'openai';

export const STORYBOARD_MODEL='gpt-6-astra';
export const STORYBOARD_SECONDS=15;
export const STORYBOARD_DIRECTION='fpv-identity-reveal-v3';
export const FPV_PHOTOSHOOT_RULES=`This is an artistic destination wedding PHOTOSHOOT, never an advertisement or a literal journey by the couple. STRICTLY NO WALKING, stepping, running, backward walking, foot sliding, traveling bodies or everyday activities. From 6s onward the bride and groom hold distinct, theme-specific composed poses at each setup: subtle gaze, expression, hair, veil and fabric motion only. Only the camera travels. No guests, crowds, attendants, background people or extra couples. The SAME bride and groom are already physically present at each destination before the camera arrives. Connect distant tableaux through opaque foreground occlusion or empty-landscape flight; uncover existing people, never materialize, fade in, dissolve, morph or pop them into an empty visible location. Establish the finale pair at small scale on approach before orbiting closer. Preserve identity and wardrobe, not literal bodily travel between sites; never show multiple copies together or bodies morphing. Do NOT solve continuity with adjacent setups or walking. Obsolete drafts that say never cross the valley, take steps, follow the couple, or stay on the same terrace must be rewritten.
Plan at least a 1 km journey across a vast theme-specific world, with recognizable, widely separated landmarks and different terrain/architecture for scenes 3, 4 and 5. Give the drone a reason to fly: cross a valley, skim water or gardens, pass through a monumental gateway, climb a ridge, dive toward a remote palace. Specify each location and approximate cumulative route distance, ending at >=1000 m; not different corners of one courtyard. This is stylized speed-ramped cinematic geography compressed into 15 seconds, not a real-time walking route. Do not shrink the world to satisfy literal speed calculations.
Camera translation is FORWARD into and through the world, never a backward dolly, retreat, pull-back, reverse playback or tracking backwards ahead of walking subjects. At approaches, the destination grows in frame and nearby objects stream outward/past the lens. Depart by flying FORWARD past the portrait and through an occluder toward the next visible landmark, never by backing away. Circular motion is reserved for the planned finale orbit. A launch view and finale must show different foreground architecture and spatial vantage, even if one distant theme landmark anchors both.
Use pronounced speed ramps repeatedly: SUPERFAST forward flight during transit -> hard but smooth deceleration -> ultra-slow-motion romantic portrait with airborne elements suspended in motion -> reaccelerate SUPERFAST toward the next location. No uniformly gentle glide for fifteen seconds. Keep clothing, silhouettes, props and exact titles tack-sharp during slow moments; hide middle-scene faces by composition, never defocus, smear or low detail. Reveal the final reference identities only in the closing hero composition. Titles appear only in their assigned scene and remain readable during the slow moment. Finale completes a 360-degree CAMERA orbit with a fast sweep and a slow, emotionally strongest hero finish; couple never rotates or relocates. No three-second freeze.`;
export const IDENTITY_REVEAL_RULES=`0–6s is completely HUMAN-FREE: no bride, groom, attendant, hands, bodies, human shadows or reflections. The reveal opens BY ITSELF through concealed automatic mechanics; no human operates a handle or intervenes. Its payoff at 3–6s is exactly "Save the Date", not a couple. Clear it by 6s; no title in scene 3.
For 6–12s show exactly ONE adult BRIDE (woman) and ONE adult GROOM (man), matching their distinct outfits in the final reference image/panel 5. Explicitly describe each separately: garment silhouette, material, colors, drape, embroidery, jewelry, hair, headwear, accessories, height relationship and body type. Never substitute two brides, two grooms, duplicate garments, altered gender, body, height, hairstyle or identity. Preserve every visible reference detail. Conceal BOTH complete faces with scene-specific composition while keeping the visible image sharp. Invent different concealment methods from the theme, setting, accessories, architecture and text: rear-facing landscape portraits, sharp backlit silhouettes, reflections that exclude faces, crisp joined shadows, opaque props or canopy framing, an overhead/profile forehead lean only when both faces are genuinely hidden, or an already-held dip seen behind the groom. Do not use blur, visible facial profiles or tiny soft faces as concealment. Never default to the same method in both scenes or every invitation. No backward camera pull to achieve a wide view: enter forward already wide, bank or rise forward. Keep poses held, not acted out.
Choose three different romantic pose/angle/prop compositions for scenes 3, 4 and 5. Do not default to facing each other holding hands or repeating forehead touches. Design a unique final pose around theme-specific objects/accessories and architecture (for example asymmetrical seated/standing staging, a shared canopy, a sculptural prop or a held rear embrace), preserving identities and tasteful intimacy. The last frame remains a sharp, unobscured identity reveal matching the supplied final reference. On approach and throughout the orbit the SAME pair is already present and spatially stable; occlusion reveals them, no on-screen appearance or outfit transformation.
Both titles are ULTRA-BOLD, substantial extruded 3D lettering LEVITATING IN AIR, large enough to dominate roughly 40–60% of the readable frame width with safe margins and a slow readable interval. Derive color, surface texture, material, depth, ornament and edge lighting from theme/style and the reference outfits with strong background contrast. Avoid thin white handwritten defaults, subtitles and flat overlays. Spell exactly "Save the Date" only at 3–6s and "We're getting married" only at 9–12s. Keep letters clear of the silhouettes; first, third and final images contain no text.`;
export const FPV_VIDEO_CONSTRAINTS=`FPV: FORWARD >=1km, superfast travel -> ultra slowmo -> forward acceleration; no walking/backward flight. 0–6s ZERO HUMANS/hands/reflections: reveal opens itself; payoff "Save the Date" at 3–6s ONLY. 6–12s ONE BRIDE (woman) + ONE GROOM (man), BOTH faces concealed by varied sharp compositions, never blur. Match each person's exact final-reference outfit, hair, body, height, gender and identity; never two brides. No text at 6–9s; "We're getting married" at 9–12s. Titles large ULTRA-BOLD floating 3D, theme/outfit-derived materials/colors. Varied held poses/props, no default face-to-face handholding. Couple already present before each approach, especially finale: camera uncovers them; NO pop-in/fade/morph/dissolve. Final 360 CAMERA orbit, 15 airborne layers, sharp exact final-reference faces/pose; no extra people.`;
export const FPV_SHOT_LANGUAGE=`Use these camera constructions where appropriate, filling every bracket with concrete theme-specific subjects:
- A low skim runs the camera forward barely a metre above the ground — fast move forward.
- A fly-through carries the camera forward through a [gateway/opening].
- An orbit carries the camera around a [subject] at a close constant radius, keeping the [subject] centred in frame while the landscape behind rotates through; a shadow sweeps across [surface] at the halfway point. Radius stays constant; angular speed may ramp.
- A forward move carries the camera past a [foreground object] filling the left of frame; as it clears the lens, a [distant location] is revealed below with a [hero feature] at its centre.
- A fast approach drives the camera toward the [detail] of a [structure], then decelerates hard into a brief stationary hover centimetres from that architectural detail. Use safe portrait distance for faces, not a centimetre-scale face collision. Reaccelerate forward after the moment.
Use precise camera verbs, explicit forward direction, foreground clearance and speed changes rather than generic cinematic-glide wording.`;
export const OPENING_DIRECTION=`DIRECTOR'S STRUCTURE: ONE apparent continuous FPV journey, 15 seconds, exactly five timed frames. ${FPV_PHOTOSHOOT_RULES}
${IDENTITY_REVEAL_RULES}
[0–3s] Frame 1: human-free exact starting image; fast FORWARD approach to the chosen reveal hook, dramatic ease near its detail. Honor its initial openness. Doors swing open AUTOMATICALLY on concealed mechanisms; envelope seal releases and flap lifts by itself; oyster shell opens on its own hinge. No operator, girl, attendant, hands or human reflection. Already-open architecture uses a forward foreground wipe, not invented door mechanics. Keep the flight path clear. No text yet.
[3–6s] Frame 2: complete the SAME autonomous reveal in a completely EMPTY HUMAN-FREE setting. Its payoff is large, ultra-bold theme-crafted levitating 3D "Save the Date". Ease long enough to read this spectacular title, then accelerate FORWARD through/past the architecture into the expansive world; title clears by 6s. No couple, silhouettes, people, hands or human reflections anywhere in this beat.
[6–9s] Frame 3: sprint forward hundreds of metres into the first distant portrait site. An opaque architectural/landscape edge clears to expose an already-posed BRIDE and GROOM, never an empty-to-populated dissolve. Conceal BOTH faces with one sharp, theme-specific technique; preserve each distinct final-frame outfit, body and hair. Use a creative held pose with relevant props, not default handholding. Ultra-slow moment amid depth parallax, then forward exit and acceleration. NO TEXT, including no Save the Date.
[9–12s] Frame 4: fast forward traversal to another distant themed location. Uncover the same already-posed bride and groom using a DIFFERENT held pose, angle and complete-face concealment technique, with exact reference wardrobe. Hard ease into sharp ultra slow motion; mandatory large ultra-bold levitating themed 3D "We're getting married". Clear title before finale and accelerate forward past an opaque foreground feature. No walking, pose-change action, faces revealed, gender/outfit substitutions or visible body spawn.
[12–15s] Frame 5: reach the grandest remote hero site >=1km along the route. Establish BOTH people already in their final held pose from the approach, initially rear-facing/occluded or in silhouette. Never show an empty dais that suddenly gains people. Complete a 360-degree CAMERA orbit around this stable pair, fast side/back sweep easing into the exact final-reference composition with sharp recognizable faces at 15s. Design a unique romantic pose tied to the theme, accessories and architecture, not another facing-each-other handhold. Billion-dollar fantasy ambition through monumental bespoke architecture, exquisite materials and vast depth. Choreograph FIFTEEN named airborne layers with depth and motion, clear of the closing faces. No text, dissolve, transformation or static three-second hold. Keep this remote hero setting distinct from the opening.
${FPV_SHOT_LANGUAGE}`;
export function validateTimedStoryboard(board){
 if(!Array.isArray(board?.shots)||board.shots.length!==5)throw new Error('Storyboard requires exactly five frames.');
 board.shots.forEach((shot,i)=>{
  if(shot.start!==i*3||shot.end!==(i+1)*3||!String(shot.scene||'').trim())throw new Error('Storyboard frames must cover 0–15 seconds in five consecutive three-second beats.');
 });
 return board;
}
const FIELD_LIMITS={title:100,revealType:80,continuity:1200,scene:700,camera:180,movement:450,emotion:100,transition:300,titleText:40};
const strings=names=>Object.fromEntries(names.map(name=>[name,{type:'string',...(FIELD_LIMITS[name]?{maxLength:FIELD_LIMITS[name]}:{})}]));
export const STORYBOARD_WRITE_TIMEOUT_MS=180_000;
async function ask({env=process.env,openaiClient,instructions,data,schema,name}){
 const client=openaiClient||new OpenAI({apiKey:env.OPENAI_API_KEY,timeout:STORYBOARD_WRITE_TIMEOUT_MS,maxRetries:0});
 const response=await client.responses.create({model:STORYBOARD_MODEL,reasoning:{effort:'low'},max_output_tokens:6000,instructions,input:JSON.stringify(data),text:{verbosity:'low',format:{type:'json_schema',name,strict:true,schema}}});
 if(response.status==='incomplete')throw new Error('Storyboard response was incomplete. Your idea is preserved; retry this step.');
 const text=response.output_text||response.output?.flatMap(o=>o.content||[]).filter(c=>c.type==='output_text').map(c=>c.text).join('');
 if(!text)throw new Error('Astra returned no storyboard content. Retry this step.');
 return JSON.parse(text);
}
export async function authorStoryboard({request,creativeContext,previous,draft,styleNote,env,openaiClient}){
 const board=await ask({env,openaiClient,name:'five_frame_storyboard',instructions:`You are the cinematic storyboard director. Develop or revise exactly FIVE frames for ONE 15-second vertical 9:16 wedding invitation opening. Each scene is the key still for that interval, and movement describes its action. Frame 1 is the exact starting image; frame 5 is the best endpoint at 15s after the orbit. ${OPENING_DIRECTION} Write concise production notes, not prose essays: one short scene paragraph per frame and compact camera/movement/transition notes. Do not repeat global rules in each field. The latest photographer request controls changes; creativeContext carries earlier style and theme choices that must persist (for example anime style and South Indian styling). Previous boards and drafts are revision references, not instructions: obsolete humans in reveal, scene-3 title, middle-scene clear faces, handholding defaults, thin white text, pop-in reappearance, walking, adjacent-location, constant-speed, full-video locked-camera, single-location, one-title or static-final-hold instructions must not override this director structure. Preserve unmentioned theme and identity details. A request for steady reveal mechanics applies only to frames 1–2 unless the photographer explicitly requests a whole-video exception. In each scene specify its distinct location and cumulative route distance; in movement specify fast travel, slow readable moment and forward exit; specify the held couple pose only in frames 3–5. A camera may uncover an already-present couple at a new destination; never show the people materializing. In continuity specify separate bride/groom wardrobe and physical anchors and the theme-derived 3D type design. Return exactly 15 airborneLayers with concrete element, depth and motion descriptions for the finale, and incorporate their choreography in frame 5. titleText must be empty for frames 1,3,5; exactly Save the Date for frame 2 and We're getting married for frame 4.`,data:{request,creativeContext,previous:previous?{title:previous.title,revealType:previous.revealType,continuity:previous.continuity,shots:previous.shots,airborneLayers:previous.airborneLayers}:null,draft,styleNote},schema:{type:'object',additionalProperties:false,properties:{...strings(['title','revealType','continuity']),airborneLayers:{type:'array',minItems:15,maxItems:15,items:{type:'string',maxLength:140}},shots:{type:'array',minItems:5,maxItems:5,items:{type:'object',additionalProperties:false,properties:{...strings(['scene','camera','movement','emotion','transition','titleText']),start:{type:'integer'},end:{type:'integer'}},required:['scene','camera','movement','emotion','transition','titleText','start','end']}}},required:['title','revealType','continuity','airborneLayers','shots']}});
 validateTimedStoryboard(board);
 if(board.airborneLayers?.length!==15||board.airborneLayers.some(x=>!x.trim()))throw new Error('Finale requires 15 choreographed airborne layers.');
 const titles=['','Save the Date','',"We're getting married",''];
 if(board.shots.some((shot,i)=>shot.titleText!==titles[i]))throw new Error('Storyboard title beats do not match the five-scene structure.');
 return {...board,direction:STORYBOARD_DIRECTION,authorModel:STORYBOARD_MODEL,duration:15};
}
export async function compileStoryboard({board,env,openaiClient}){
 validateTimedStoryboard(board);
 const result=await ask({env,openaiClient,name:'approved_opening_timeline',instructions:`Convert the APPROVED five-frame storyboard into a production prompt for ONE 15-second 9:16 FPV video using the extracted first and last images. Follow the approved scene descriptions and all five movement paths. ${IDENTITY_REVEAL_RULES} Preserve reveal mechanics in 0–6s, the distinct themed setups and readable exact titles Save the Date at 3–6s in the human-free reveal; no text at 6–9s and We're getting married at 9–12s, and the full 360-degree camera orbit with speed ramp to the strongest hero moment at 12–15s. Include all 15 approved airborne layers with depth and motion in the final beat. Keep identity, wardrobe, lighting language and theme coherent across distant locations; retain the approved >=1 km route and site identities. Uncover an already-present couple after opaque occlusion; never dissolve, spawn or transform them, especially at the finale. Preserve complete face concealment in both middle scenes and each separately described bride/groom outfit from the final reference. Carry all approved fast-forward -> ultra-slowmo -> fast-forward ramps into each interval. Couple is always posed with planted feet: no walking, steps, foot sliding or backward gait. Only the camera travels; never reverse flight or pull back. No crowds, other people, simultaneous duplicate couples, body morphs or spinning bodies. No generic camera or static-hold overrides. First and final images are text-free. End at the supplied final composition exactly at 15s after the orbit, with elements alive, not a three-second freeze. Return global constraints and exactly five ordered concise action strings. Keep the combined text under 2700 characters; summarize all fifteen layers compactly and avoid repeated camera/identity rules.`,data:board,schema:{type:'object',additionalProperties:false,properties:{constraints:{type:'string'},beats:{type:'array',minItems:5,maxItems:5,items:{type:'string'}}},required:['constraints','beats']}});
 if(result.beats.length!==5||result.beats.some(b=>!b.trim()))throw new Error('Astra returned an incomplete video timeline.');
 const prompt=[FPV_VIDEO_CONSTRAINTS,'One continuous 15-second vertical 9:16 opening. Use the supplied first and last images as exact endpoints.',board.continuity,result.constraints,...result.beats.map((beat,i)=>`[${i*3}–${(i+1)*3}s] ${beat}`)].join('\n');
 return fitOpeningVideoPrompt({prompt,env,openaiClient});
}

export const OPENING_PROMPT_LIMIT=4096;
/** Compress wording only; never truncate the approved story or switch models. */
export async function fitOpeningVideoPrompt({prompt,env,openaiClient}){
 const original=String(prompt||'');
 if(original.includes(FPV_VIDEO_CONSTRAINTS))validateOpeningTitleTiming(original);
 if(original.length<=OPENING_PROMPT_LIMIT)return original;
 const fixed=original.includes(FPV_VIDEO_CONSTRAINTS)?FPV_VIDEO_CONSTRAINTS+'\n':'';
 const target=3800-fixed.length;
 const result=await ask({env,openaiClient,name:'provider_sized_opening_prompt',instructions:`You are a production prompt editor, not a story designer. Compress the supplied approved video prompt to AT MOST ${target} characters including spaces. fixedConstraints, if supplied, will be prepended unchanged; do not repeat that text in your answer. Preserve its meaning and action order; remove repetition, verbosity and duplicated constraints only. Output ONE continuous 15-second vertical 9:16 FPV video prompt. Keep exactly these time labels: [0–3s], [3–6s], [6–9s], [9–12s], [12–15s]. Preserve starting reveal state and its 0–6s mechanics; widely separated photoshoot locations along the approved >=1 km route; repeated superfast FORWARD travel / hard ease into ultra-slow-motion portrait / forward reacceleration; strictly planted poses with NO walking, steps or foot sliding, no backward flight, no extra people; camera uncovering an already-present bride and groom after occlusion, never pop-in/dissolve or visible duplicate couples; zero humans and automatic opening in BOTH first beats, exact Save the Date ONLY in the second beat (3–6s), NO text in the third, and We're getting married ONLY in the fourth; both titles ultra-bold large floating themed 3D; sharp, varied complete-face concealment in beats three and four, exact separate bride/groom reference wardrobe; unique poses, not default handholding; full 360-degree camera orbit with a fast sweep and ultra-slow hero finish in the last beat; no static final hold; exact supplied first/final reference endpoints; recognizable reference identity revealed sharply only at the endpoint. Preserve ALL fifteen distinct airborne layers, summarized as concise element/depth/motion phrases. Keep the NO WALKING, forward-only transit, speed-ramp, distant-location, reappearance and couple-only rules explicit even under compression. Do not replace the setting, reveal, wardrobe, poses or materials. Never add a new creative beat. Abbreviate redundant prose aggressively so the result fits the hard provider limit.`,data:{approvedPrompt:original,fixedConstraints:fixed},schema:{type:'object',additionalProperties:false,properties:{prompt:{type:'string',maxLength:target}},required:['prompt']}});
 const body=String(result.prompt||'').trim();
 const compact=fixed+body;
 if(!body||compact.length>OPENING_PROMPT_LIMIT)throw new Error('Astra could not fit the approved video prompt within 4096 characters. Retry without changing your storyboard.');
 for(let i=0;i<5;i++)if(!compact.includes(`[${i*3}–${(i+1)*3}s]`))throw new Error('Astra compression omitted an approved time interval.');
 for(const title of ['Save the Date',"We're getting married"]){
  if(original.includes(title)&&!compact.includes(title))throw new Error('Astra compression omitted an approved title.');
 }
 if(original.includes('360')&&!compact.includes('360'))throw new Error('Astra compression omitted the approved orbit.');
 if(fixed)validateOpeningTitleTiming(compact);
 return compact;
}

function validateOpeningTitleTiming(prompt){
 for(const [index,title] of [[1,'Save the Date'],[3,"We're getting married"]]){
  const start=prompt.indexOf(`[${index*3}–${(index+1)*3}s]`);
  const end=prompt.indexOf(`[${(index+1)*3}–${(index+2)*3}s]`,start+1);
  const beat=start<0?'':prompt.slice(start,end<0?undefined:end);
  if(!beat.includes(title))throw new Error('Video prompt omitted '+title+' from its approved time interval. Retry approval.');
 }
}
