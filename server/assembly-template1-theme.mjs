// Template 1 theme: generate the per-clone CSS block and patch the registries iteration 1 edited by hand.
// Standing placement: mid-sky overlay, thin scrim, plate1 on sections 1/2/3/5, plate2 on the TAG cluster,
// no frame on Welcome / Our Moments / Timeline / Dress Code / RSVP / footer.

function rgba(hex,alpha){
 const m=String(hex||'').replace('#','');
 const r=parseInt(m.slice(0,2),16),g=parseInt(m.slice(2,4),16),b=parseInt(m.slice(4,6),16);
 return 'rgba('+r+','+g+','+b+','+alpha+')';
}

export function normalizePalette(p={}){
 return {
  primary:p.primary||'#9B2158',
  primaryDeep:p.primaryDeep||'#6E1540',
  bloom:p.bloom||'#C45A8A',
  secondary:p.secondary||'#3F5C55',
  secondaryDeep:p.secondaryDeep||'#2C4440',
  cream:p.cream||'#F7F1E8',
  primaryWash:p.primaryWash||'#F3E0EA',
  secondaryWash:p.secondaryWash||'#E4EDE8',
  ink:p.ink||'#3A2428'
 };
}

export function themeCss(id,palette,{displayName=''}={}){
 const c=normalizePalette(palette);
 const s='.invitation-page.theme-'+id;
 const cream=c.cream;
 const glow=(a)=>rgba(cream,a);
 return `/* ${id}${displayName?' / '+displayName:''}: Template 1 — pin palette, mid-sky overlay, thin plates. Generated by /assembly. */
${s}{
 --t1-primary:${c.primary};
 --t1-primary-deep:${c.primaryDeep};
 --t1-secondary:${c.secondary};
 --t1-secondary-deep:${c.secondaryDeep};
 --t1-bloom:${c.bloom};
 --t1-cream:${cream};
 --t1-primary-wash:${c.primaryWash};
 --t1-secondary-wash:${c.secondaryWash};
 --t1-ink:${c.ink};
 --t1-scrim:linear-gradient(180deg,rgba(0,0,0,.08) 0%,rgba(0,0,0,0) 10%,rgba(0,0,0,0) 90%,rgba(0,0,0,.08) 100%),linear-gradient(90deg,rgba(0,0,0,.06) 0%,rgba(0,0,0,0) 10%,rgba(0,0,0,0) 90%,rgba(0,0,0,.06) 100%);
 --invite-color:${c.primary};
 --invite-paper:${cream};
 --invite-ink:${c.ink};
 background:${cream};
 color:${c.ink};
}
${s} .invitation-hero{height:100svh;min-height:100svh}
${s} .couple-overlay{justify-content:center;align-items:center;text-align:center;padding:4svh 13% 50svh 13%;overflow:hidden;background:transparent;box-sizing:border-box;color:${c.primary};text-shadow:0 1px 0 ${glow('.92')},0 2px 10px ${glow('.78')},0 0 22px ${glow('.46')}}
${s} .hero-copy{width:100%;max-width:100%;margin:0;padding:8px 10px;display:flex;flex-direction:column;align-items:center;text-align:center;box-sizing:border-box;background:radial-gradient(ellipse at 50% 42%,${glow('.48')} 0%,${glow('.16')} 46%,${glow('0')} 74%)}
${s} .hero-person{width:100%;text-align:center}
${s} .hero-heart{display:block;width:20px;height:20px;margin:0 auto 8px;color:${c.primary};fill:${c.primary}}
${s} .hero-copy .welcome-title{font-size:clamp(18px,5.4vw,26px);line-height:1.25;margin:0 auto 6px;max-width:20em;text-align:center;color:${c.secondary}}
${s} .hero-divider{margin:6px auto 10px;gap:10px;justify-content:center;color:${c.primary}}
${s} .hero-divider i{width:40px;background:${c.bloom};opacity:.9}
${s} .couple-overlay h1,${s} .hero-person h1{font-size:clamp(52px,16vw,76px);line-height:.95;margin:0;width:100%;text-align:center;color:${c.primaryDeep}}
${s} .hero-person .couple-subtext{color:${c.secondary}}
${s} .bride-person h1{color:${c.primary}}
${s} .bride-person .couple-subtext{color:${c.secondary}}
${s} .hero-copy .couple-subtext,${s} .couple-subtext{font-size:clamp(14px,3.8vw,17px);line-height:1.3;margin:4px auto 8px;padding:0;max-width:22em;text-align:center}
${s} .hero-copy .couple-amp,${s} .couple-amp{font-size:clamp(24px,7vw,34px);line-height:1;margin:4px 0 8px;text-align:center;color:${c.primary};opacity:1}
${s} .hero-scroll{position:absolute;bottom:44svh;left:50%;right:auto;translate:-50% 0;margin:0;font-size:11px;color:${c.secondary}}
@media(max-width:480px){
 ${s} .couple-overlay{padding:5svh 14% 50svh 14%}
 ${s} .couple-overlay h1,${s} .hero-person h1{font-size:clamp(48px,15.5vw,68px)}
 ${s} .hero-copy .welcome-title{font-size:clamp(17px,5.2vw,22px)}
 ${s} .hero-copy .couple-subtext,${s} .couple-subtext{font-size:clamp(14px,3.9vw,16px)}
}
@media(min-aspect-ratio:3/4){${s} .couple-overlay{padding:4svh 18% 46svh 18%}}
@media(max-height:640px){
 ${s} .couple-overlay{padding:3svh 12% 48svh 12%}
 ${s} .couple-overlay h1,${s} .hero-person h1{font-size:clamp(40px,11vw,56px)}
 ${s} .hero-copy .welcome-title{font-size:16px}
 ${s} .hero-copy .couple-subtext{font-size:13px}
}
${s}>.sound-toggle,
${s}>.language-toggle,
${s}>.use-design{background:${glow('.92')};color:${c.primary};border-color:${rgba(c.bloom,'.55')}}
${s} .invite-welcome,
${s} .invite-section{
 max-width:none;width:100%;margin:0;box-sizing:border-box;position:relative;
 background-color:${cream};background-size:cover;background-position:center;background-repeat:no-repeat;
 color:${c.ink};padding:72px 24px
}
${s} .invite-welcome{padding:80px 24px;background-image:none;background:${cream};color:${c.ink}}
${s} .invite-plate-1{background-image:var(--t1-scrim),url('/assets/${id}-section-1.jpg')}
${s} .invite-plate-2{background-image:var(--t1-scrim),url('/assets/${id}-section-2.jpg')}
${s} .invite-plate-3{background-image:var(--t1-scrim),url('/assets/${id}-section-3.jpg')}
${s} .invite-plate-4{background-image:var(--t1-scrim),url('/assets/${id}-section-4.jpg')}
${s} .invite-plate-5{background-image:var(--t1-scrim),url('/assets/${id}-section-5.jpg')}
${s} .invite-cluster{max-width:none;width:100%;background-color:${cream};background-size:cover;background-position:center;background-repeat:no-repeat;background-image:var(--t1-scrim),url('/assets/${id}-section-4.jpg')}
${s} .invite-cluster .invite-section{background-image:none;background-color:transparent;padding:40px max(28px,12%)}
${s} .invite-cluster .invite-section:first-child{padding-top:80px}
${s} .invite-cluster .invite-section:last-child{padding-bottom:80px}
${s} .invite-section h2{color:${c.primary};text-shadow:0 1px 2px rgba(0,0,0,.16)}
${s} .invite-plate-3 h2,
${s} .invite-plate-5 h2{color:${c.secondary}}
${s} .invite-welcome p,
${s} .invite-section p,
${s} .invite-section small{color:${c.ink};text-shadow:0 1px 2px rgba(0,0,0,.12)}
${s} .invite-divider{color:${c.bloom};opacity:.95}
${s} .invite-section h3,
${s} .timeline h3{color:${c.primary}}
${s} .invite-plate-3 h3,
${s} .invite-plate-5 h3{color:${c.secondary}}
${s} .fmi-button{background:${c.primary};border-color:${c.bloom};color:${cream}}
${s} .fmi-button:hover{background:${c.secondary};border-color:${c.secondary};color:${cream}}
${s} .fmi-button.outline{background:transparent;color:${c.primary};border-color:${c.bloom}}
${s} .scratch-heart{background:${c.primaryWash}}
${s} .scratch-date{color:${c.primary}}
${s} .scratch-date em{color:${c.secondary}}
${s} .calendar-menu{background:${cream};border-color:${c.bloom};color:${c.ink}}
${s} .calendar-menu a:hover,
${s} .calendar-menu button:hover{background:${c.secondaryWash}}
${s} .countdown>div{background:${glow('.94')};border:1px solid ${c.bloom};border-radius:10px;padding:12px 14px;color:${c.primary}}
${s} .countdown strong{color:${c.primary}}
${s} .countdown span{color:${c.secondary}}
${s} .timeline{border-left-color:${c.bloom}}
${s} .timeline>div:before{background:${c.primary}}
${s} .photo-slideshow{box-shadow:0 12px 36px ${rgba(c.primaryDeep,'.18')};border:1px solid ${rgba(c.bloom,'.45')}}
${s} .fmi-form input,
${s} .fmi-form select,
${s} .fmi-form textarea{background:${cream};border-color:${c.bloom};color:${c.ink}}
${s} .fmi-form label{color:${c.primaryDeep}}
${s} .form-success{background:${rgba(c.secondaryWash,'.92')};border-color:${c.bloom};color:${c.secondaryDeep}}
${s} .invite-credit{background:${cream};color:${c.primary};border-color:${c.bloom}}
${s} .invite-credit-line,
${s} .invite-credit-line a{color:${c.secondary}}
`;
}

export function patchThemeCss(source,id,palette,opts={}){
 if(source.includes('.invitation-page.theme-'+id+'{'))return source;
 return source.replace(/\s*$/,'\n')+themeCss(id,palette,opts);
}

export function patchPreviewDefaults(source,id,{groom,bride,groomDetails,brideDetails,photos,faceSwap}={}){
 if(new RegExp("'"+id+"':\\{groom:").test(source))return source;
 const marker='const previewDefaults:Record<string,Partial<InviteData>>={';
 const start=source.indexOf(marker);
 if(start<0)throw new Error('Could not find previewDefaults in Invitation.tsx');
 const end=source.indexOf('\n};',start);
 if(end<0)throw new Error('Could not find previewDefaults end');
 const esc=v=>String(v||'').replace(/\\/g,'\\\\').replace(/'/g,"\\'").replace(/\n/g,'\\n');
 let fields="groom:'"+esc(groom)+"',bride:'"+esc(bride)+"',groomDetails:'"+esc(groomDetails)+"',brideDetails:'"+esc(brideDetails)+"'";
 if(Array.isArray(photos)&&photos.length>=2){
  fields+=",photos:['"+esc(photos[0])+"','"+esc(photos[1])+"']";
 }
 if(faceSwap&&typeof faceSwap==='object'){
  const couple=esc(faceSwap.coupleUrl||'');
  const brideU=esc(faceSwap.brideUrl||photos?.[0]||'');
  const groomU=esc(faceSwap.groomUrl||photos?.[1]||'');
  if(couple||brideU||groomU){
   fields+=",faceSwap:{status:'ready',coupleUrl:'"+couple+"',brideUrl:'"+brideU+"',groomUrl:'"+groomU+"'}";
  }
 }
 const line=",\n '"+id+"':{"+fields+"}";
 return source.slice(0,end)+line+source.slice(end);
}

export function patchMusicTracks(source,url){
 const match=source.match(/export const musicTracks=new Set\(\[([^\]]*)\]\);/);
 if(!match)throw new Error('Could not find musicTracks in core.mjs');
 const existing=[...match[1].matchAll(/'([^']*)'/g)].map(m=>m[1]);
 if(existing.includes(url))return source;
 existing.push(url);
 return source.replace(match[0],'export const musicTracks=new Set(['+existing.map(v=>"'"+v+"'").join(',')+']);');
}

export function patchDataRow(source,id,{music,musicName,color}){
 const needle="id:'"+id+"'";
 const idx=source.indexOf(needle);
 if(idx<0)throw new Error('Template not found in data.ts: '+id);
 const blockStart=source.lastIndexOf('{',idx);
 const blockEnd=source.indexOf('}',idx);
 let block=source.slice(blockStart,blockEnd+1);
 const esc=v=>String(v).replace(/\\/g,'\\\\').replace(/'/g,"\\'");
 if(music){
  if(/,music:'[^']*'/.test(block))block=block.replace(/,music:'[^']*'/,",music:'"+esc(music)+"'");
  else block=block.replace(/,badge:/,",music:'"+esc(music)+"',badge:");
 }
 if(musicName){
  if(/,musicName:'[^']*'/.test(block))block=block.replace(/,musicName:'[^']*'/,",musicName:'"+esc(musicName)+"'");
  else block=block.replace(/,badge:/,",musicName:'"+esc(musicName)+"',badge:");
 }
 if(color&&/^#[0-9A-Fa-f]{6}$/.test(color))block=block.replace(/color:'#[0-9A-Fa-f]{3,8}'/,"color:'"+color+"'");
 return source.slice(0,blockStart)+block+source.slice(blockEnd+1);
}

/** App.tsx editor "Background music" select: add/extend the option for this clone's track and keep the "Saved music" exclusion list in sync. */
export function patchAppMusicOption(source,id,url,label){
 let next=source;
 const optionNeedle='<option value="'+url+'">';
 const optIdx=next.indexOf(optionNeedle);
 const cond="data.template==='"+id+"'";
 if(optIdx>=0){
  const condStart=next.lastIndexOf('{(',optIdx);
  const condEnd=next.indexOf(')&&',condStart);
  if(condStart<0||condEnd<0||condEnd>optIdx)throw new Error('Could not locate music option condition in App.tsx');
  const condition=next.slice(condStart+2,condEnd);
  if(!condition.includes(cond))next=next.slice(0,condStart+2)+cond+'||'+condition+next.slice(condEnd);
 }else{
  const anchor="{data.music&&![";
  const at=next.indexOf(anchor);
  if(at<0)throw new Error('Could not find music select anchor in App.tsx');
  const esc=v=>String(v).replace(/[&<>]/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[ch]));
  const option="{("+cond+"||data.music==='"+url+"')&&<option value=\""+url+"\">"+esc(label)+"</option>}";
  next=next.slice(0,at)+option+next.slice(at);
 }
 const listMatch=next.match(/\{data\.music&&!\[([^\]]*)\]\.includes\(data\.music\)/);
 if(!listMatch)throw new Error('Could not find saved-music exclusion list in App.tsx');
 const list=[...listMatch[1].matchAll(/'([^']*)'/g)].map(m=>m[1]);
 if(!list.includes(url)){
  list.push(url);
  next=next.replace(listMatch[0],'{data.music&&!['+list.map(v=>"'"+v+"'").join(',')+'].includes(data.music)');
 }
 return next;
}
